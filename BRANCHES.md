# 分支说明

本文件记录 2026-09-23 仓库整合后的最终拓扑与归档关系。

## 当前拓扑

`main` 是**唯一正式成果入口**，四问成果在同一棵树内，各自独立目录：

```text
main (唯一分支)
├── Q1/   第一问：单点运输、组批、能耗/时间
├── Q2/   第二问：多点运输调度与资源分配
├── Q3/   第三问：通信盲区检测与中继调度
└── Q4/   第四问：分区与独立资源配置
```

不再维护永久 q1～q4 成果分支链；后续开发从 `main` 创建短期工作分支，复核合并后删除。

## 整合方式

整合采用「快进」：main 原为初始规范提交 `8a55043`，q4 是其下游完整集成分支，main 直接快进到 q4 的完整成果，随后新增整合提交（根文档 + 本说明 + 整合脚本）。四问目录 `Q1/Q2/Q3/Q4` 与整合前 q4 对应目录**逐字节一致**，代码、参数、结果、验证与图表均未改动。

## 目录依赖关系

- `Q1/code/main.py` 复用 `Q2/code/prepare_data.py` 的 `Terrain` 做 UTM 直线逆投影 DEM 遍历（240 航段 0 差异，CASE A）；**不能删除 Q2 让 Q1“独立”**。
- `Q3` 依赖 `Q2`（运输轨迹与 DEM 几何）；`Q4` 依赖 `Q2/Q3`（运输架次与中继调度）。这些依赖是题面与复现所必需，予以保留。

## 归档标签（archive/consolidation）

2026-09-23 整合前，为各旧开发分支尖端创建归档标签（带时间戳，均为标注标签，指向整合前提交）：

| 标签 | 指向提交 | 说明 |
|---|---|---|
| `archive/consolidation/main-20260923T120301Z` | `8a55043` | 整合前 main（仅协作规范） |
| `archive/consolidation/q1-20260923T120301Z` | `fee99f9` | 第一问开发分支尖端 |
| `archive/consolidation/q2-20260923T120301Z` | `81915bb` | 第二问开发分支尖端 |
| `archive/consolidation/q3-20260923T120301Z` | `274c677` | 第三问开发分支尖端（三架增配 100%） |
| `archive/consolidation/q4-20260923T120301Z` | `59d1281` | 第四问开发分支尖端（整合快进源） |

取回旧分支尖端：`git checkout archive/consolidation/q3-20260923T120301Z`。

更早的历史标签（`pre-tidy/*`、`post-tidy/*`、`archive/audit-*`）仍保留，指向更早的整理与审计节点。

## 结果边界（重要）

- **第三问**：原题两架中继最佳已找到 407 个中断样本（覆盖率 98.88%），`global_infeasibility_proven=false`，未证明全局不可行；三架增配 R03 在 1 秒网格及 LOS 15/10/5 米检查下通过（0 中断、100%）。
- **第四问**：结果适用于增配 R03 情景，不适用于原题两架中继。
- 统一 `main` 是分支与成果入口整理，**不代表论文证据审查已通过**。

## 已删除的远端分支

整合后删除远端开发分支 `q1`、`q2`、`q3`、`q4`（其尖端均已由上方归档标签保留）。`main` 与全部历史标签保留。删除前逐分支复核：远端尖端与归档提交一致、是 main 的祖先、归档标签已存在于远端。

## 复现整合

整合脚本见 `consolidate_main.py`，完整复现命令见该脚本顶部注释。
