# Q3 严格通信可行性复算

源码基线：`698d7d9ca78fe95384a33e2c228b9026c6023053`。原验证已实际复现：423 / 36351 中断样本，98.8363% 覆盖。

Q2 的 26 个架次、组批、路线、开始时间保持不变；本次只修改 Q3。所有正式数字统一为 1 秒时间网格、10 米 LOS，以重新读取 CSV 后的独立验证为准。

## 资源情景结果

| 情景 | 验证状态 | 中断样本秒 | 覆盖率 | 能耗 kWh |
|---|---|---:|---:|---:|
| 2 架（原题） | FAIL | 407 | 98.880361% | 4.226613 |
| 3 架（增配） | PASS | 0 | 100.000000% | 6.471971 |

原始候选集合内、声明的单固定悬停架次模型下，两点枚举的最小中断为 409；位置/高度局部细化后的最佳已找到方案为 407。

硬可行约束：`uncovered_samples == 0` 且其余时序、回传、电量与资源检查全部通过。未达到零中断的方案不可标为严格可行。没有全局不可行性证明：搜索不覆盖任意连续空间位置及多架次换位策略。

三架情景增配 R03，超出题面原有两架无人机资源；不能据此宣布原题两架问题已解决。

## 最终保存方案

最终选择 3 架，状态 PASS；这是 1 秒网格验证结果，不是采样时刻之间的连续时间证明。

| 中继 | UTM x / y (m) | 海拔 / AGL (m) | 服务窗口 [s,e) 秒 | 能耗 kWh | 返航 SOC |
|---|---|---|---|---:|---:|
| R01 | 315675.159 / 2550876.435 | 920.210 / 300.000 | [816.0, 6611.5) | 2.172152 | 32.120% |
| R02 | 322575.159 / 2546026.435 | 662.347 / 300.000 | [745.0, 6611.5) | 2.054461 | 35.798% |
| R03 | 314175.159 / 2553526.435 | 975.402 / 300.000 | [1033.0, 6611.5) | 2.245358 | 29.833% |

## LOS 敏感性

| LOS 间距 | 中断样本秒 | 覆盖率 | 状态 |
|---:|---:|---:|---|
| 15 m | 0 | 100.000000% | PASS |
| 10 m | 0 | 100.000000% | PASS |
| 5 m | 0 | 100.000000% | PASS |

通过 15/10/5 米三种离散 LOS 检查；不构成其他净空、DEM 误差、链路预算或连续时空扰动下的鲁棒性证明。

中断秒数为逐运输机样本累计值，不是多个架次中断在墙钟时间轴上的并集长度。完整区间见 `results/q3_final_blackout_intervals.csv`；无中断时仍保留 CSV 表头。

## 复现与文件

`python Q3/code/solve.py`：重做严格搜索、两种资源情景验证与敏感性；`python Q3/code/validate.py`：按正式 summary 的资源情景重新验证。可用 `--max-relays 2` 强制检查原题资源限制。

方法、假设、搜索范围与能耗模型见 [METHOD_STRICT.md](METHOD_STRICT.md)。沿用题面通信参数及原仓库中继飞行/能耗假设；无多跳，AGL 上限 300 米，能源组件可用 3.2 kWh，保留电量 20%，最多 6 组组件。正式结果不再使用旧的 5 秒覆盖矩阵。

必需输出：`q3_feasibility_2relay.json`、`q3_feasibility_3relay.json`、`q3_final_schedule.csv`、`q3_final_validation.json`、`q3_final_blackout_intervals.csv`、`q3_los_resolution_sensitivity.csv`。另保存两种情景各自的调度 CSV。

旧 figures 与 q3_sensitivity.csv 是历史分析，不是本次最终结果，不能与本次表格混用。

## Q4 影响

采用新 Q3 调度后，Q4 必须重新核算依赖 Q3 的资源、能源组件和调度结果；若采用 R03，还须显式声明增配资源。此次未修改或运行 Q4。
