# 上传规范

比赛期间只坚持一条工作链：在自己的问题分支工作 → 运行并更新成果说明 → 交叉复核 → 合并 main。

## 1. 分支与文件归属

- main 只接收稳定、已复核的阶段成果和最终版本；初始化规范、原始题目和原始数据由协调人统一上传。
- 按实际题目数创建 q1、q2、q3、q4 等小写分支，目录对应大写 Q1、Q2、Q3、Q4。每问一个负责人，不要求为每次实验新开分支。
- 每人只改自己负责的 Qx 目录。需要改队友文件时先联系负责人，由负责人修改或双方确认改动；禁止直接替换、批量覆盖或删除队友文件。
- 根 README、原始资料和公共代码由协调人统筹，避免多人同时修改。跨问题依赖写明来源文件和采用的提交编号。

## 2. 原件与数据

- 题目、附件原件放 `题目与附件/`，原始数据放 `data/raw/`，保留原文件名，禁止就地清洗、覆盖或删除。
- 清洗、筛选、补值、单位换算等由代码生成，输出到本问 `results/`，README 记录来源、处理规则和数据规模。
- 官方修订文件另存带日期或版本号的文件，记录新旧关系，保留原件。相同名称的附件用不同来源子目录隔离。
- 不上传密码、API 密钥、令牌、带凭证的链接、个人隐私资料或无权共享的数据；配置用不含真实值的 `.env.example`。误传密钥立即撤销或轮换，并通知队友协调清理历史，仅删除当前文件不够。

## 3. 每次成果上传

- `code/`：入口代码、必要模块、依赖版本或 requirements.txt；使用相对路径，不依赖某人的电脑盘符。
- `results/`：对应代码的数值结果、表格和简要运行记录；记录运行命令、数据版本、参数、随机种子（如有）、环境和运行时间。
- `figures/`：对应结果的图，坐标、单位、图例清楚；论文图尽量同时提供清晰 PNG 与矢量 PDF/SVG（适用时）。
- `README.md`：同步填写目标、方法、处理、运行方式、结果、论文图表、状态和待办。明确哪些文件供论文使用，哪些是探索性结果。
- 一个可运行成果的代码、结果、图和说明尽量同次提交；长任务可以先提交标记为 WIP 的代码，但不得冒充已验证成果或合并 main。
- 不上传虚拟环境、缓存、临时下载和重复的大文件。确需共享超出常规 Git 承载范围的数据时，先由三人约定共享位置或 Git LFS，并在 README 记录取得方式，不能只留下本机路径。

## 4. 命名与提交

文件使用“问题 + 内容/方法 + 必要版本”的稳定名称，如 `q2_metrics.csv`、`q2_residuals.png`、`q2_model_comparison_v02.csv`。中文说明文件可使用中文名。禁止 `final_new2`、`最终版_真的最终版`、`test123` 等无法判断含义的名字。

日常迭代优先更新同名文件，用 Git 历史保留版本；只有需要同时比较不同方案时才使用明确的模型名或 v01/v02，并在 README 解释区别。

提交格式：`类型(范围): 做了什么`。类型用 feat（新增）、fix（修正）、docs（说明）、results（结果）、chore（整理）；范围用 q1/q2/q3/q4/shared。例：

```text
feat(q2): 新增整数规划模型并附基线结果
fix(q1): 修正时间单位并重算结果与图表
docs(q3): 补充独立复核记录和论文图表索引
```

一次提交说明一个清楚的目的；不要只写“更新”“123”。提交前检查暂存区，确认没有队友文件、密钥或无关大文件。

## 5. AI 代码与结论复核

- AI 生成或改写的代码必须真实运行，附运行结果及运行方式；AI 声称“已验证”不算运行证据。
- 至少经过人工或第二个 AI 复核，在本问 README 记录复核者/工具、日期、检查项、发现的问题及修正结果。第二 AI 要实际检查代码、假设和结果，而非复述第一份回答；负责人最终确认。
- 重大模型结论（最优方案、显著优劣、关键预测、敏感性结论等）至少进行一次独立复核，可由另一名队友或独立 AI 使用另一种计算、简化算例、手算、替代模型、约束检查或边界测试验证。
- 原样重跑同一代码只能证明可重复，不能替代独立复核。记录核对数值、误差或容差、是否支持结论；不一致时标记“待解决”，论文不得当作确定结论引用。

## 6. 合并 main 的条件

负责人发起本问分支到 main 的合并请求，并在说明里简述：改动、运行结果、复核方式、剩余限制。另一位队友检查以下内容后再合并：

- 代码按 README 可运行；结果与图来自本次代码及指定数据。
- README 必填项完整；阶段成果标“已验证（阶段）”，最终成果标“最终”。
- AI 代码附运行与复核记录；重大结论完成独立复核，无未解决的关键矛盾。
- 论文图表链接有效、数值和单位一致、限制已说明；无原件覆盖、队友文件误改、敏感信息或无关文件。

稳定阶段成果允许有后续优化待办，但不能有影响当前结论的未解决错误。发现 main 的结果有误时立即通知论文手，在问题分支修正并重跑、复核，再合并更正。

GitHub 合并方式优先使用普通合并（Create a merge commit），问题分支在比赛期间保留，继续用于迭代。这里是团队约定，不代表已配置 GitHub 强制分支保护。

## 7. 同步与冲突

工作前先拉取自己分支；需要吸收已合并成果时，在保存并提交本地修改后执行（以 q2 为例）：

```bash
git switch q2
git fetch origin
git merge origin/main
```

发生冲突先停下，与相关文件负责人逐项确认，再编辑保留正确内容；禁止一键选择“全部用我的/对方的”覆盖未知改动。二进制图表、表格无法逐行合并时，由负责人从确认后的代码重新生成。解决后重新运行受影响部分，提交并推送。

不确定时用 `git merge --abort` 退出这次合并，再协调处理。不要强制推送共享分支，不要用硬重置丢弃队友提交。推送被拒绝时先获取远程改动并正常合并。

## 8. 旧版本与论文定稿

- 已提交的旧版本保留在 Git 历史中，不再复制多个“最终”文件夹。
- 对仍需比较的方案保留明确命名的文件及说明；确认不用的本问文件可正常删除并提交，写明原因，不改写共享历史。
- 删除未提交实验前先确认无需保留；队友文件和原始资料不得自行删除。
- 论文手以 main 的每问 README 为入口，按“论文图表清单”取文件；引用时记录提交编号。最终提交前统一核对摘要、正文、表格和图中的数值。
- 最终版本可由协调人创建 `submission-v1` 标签留档；后续修订使用新标签，不移动旧标签。不另建 deliverables 重复复制成果。
