# Q4 分区方案审计：为什么最优解是 14+1 / 13+1+1

> 审计结论：**结果数学正确、无编码错误、无目标方向错误**。14+1 / 13+1+1 的极端不均衡是「库存缺口最小」这一字典序目标的**直接、必然**结果，不是 bug。原题并未要求「最小化组间工作量均衡」，只要求「给出方案并从多维度比较」。模型无需修改，但论文表述必须诚实说明这一权衡。

## 1. 原题要求（问题四原文）

> 分别将 15 个服务区划分为 2 个和 3 个任务组……每个服务区必须且只能属于一个任务组，每个任务组至少包含一个服务区。分区过程中保持问题三已经确定的货箱组批、服务区访问顺序、运输与中继任务安排及通信保障关系不变；若同一运输架次同时涉及多个服务区，则这些服务区应划入同一任务组。各任务组仅承担本组……任务，并分别核算独立执行所需的运输无人机、共享电池、中继无人机和中继能源组件数量，任务执行期间各类资源不得跨组调配。……分别给出划分为 2 个和 3 个任务组时的任务分区及资源配置方案，并从**资源配置规模、资源冗余、组间工作量均衡以及与现有库存之间的资源缺口**等方面进行比较，分析两种分区方式的特点。若某一分区方案所需资源超过现有库存，进一步给出相应的资源缺口及其原因。

关键点：题目要求的是「**给出方案 + 多维度比较**」，**并没有**把「组间工作量均衡」规定为必须最小化的硬目标。

## 2. 当前目标函数

`Q4/code/q4_solve.py` 第 243–244 行，字典序：

```python
scored.sort(key=lambda x: (x["gap_sum"], x["total_res"], x["imbalance"]))
```

1. **库存缺口最小**（`gap_sum`）
2. 总资源配置/冗余最小（`total_res`）
3. 工作量不均衡最小（`imbalance`，最后才考虑）

## 3. 当前约束

- 每服务区恰好属于一个组；
- 无空组；
- 同一运输架次访问的多个服务区必须同组（12 个不可拆块）；
- 资源**不复用**：各组独立配置运输无人机/电池/中继/组件，组间不共享。

## 4. 2 组最优方案（gap=2）

| 组 | 块 | 服务区 | 货箱 | 架次 | 运输机 | 电池 | 中继 | 组件 |
|---|---|---|---|---|---|---|---|---|
| 组1 | B01–B07,B09–B12 | 14 | 77 | 25 | A4 B2 C2 | A6 B4 C4 | 2 | 3 |
| 组2 | B08 | S011 | 3 | 1 | B1 | B1 | 0 | 0 |

缺口 = B 型运输机 +1、B 型电池 +1（共 2）。

## 5. 3 组最优方案（gap=4）

| 组 | 块 | 服务区 | 货箱 | 架次 | 中继 | 组件 |
|---|---|---|---|---|---|---|---|
| 组1 | B01–B06,B09–B12 | 13 | 74 | 24 | 2 | 3 |
| 组2 | B07 | S009 | 3 | 1 | 1 | 1 |
| 组3 | B08 | S011 | 3 | 1 | 0 | 0 |

缺口 = 运输机 +2（A1+B1）、B 电池 +1、中继 +1（共 4）。

## 6. 为什么出现 14+1 / 13+1+1（数学原因）

1. **集中式（1 组）恰好用完库存**：8 运输机 / 14 电池 / 2 中继 / 3 组件，缺口 = 0。
2. **分区后资源不复用**：每个组都必须单独配一套无人机/电池。因此只要「切一刀」，必然产生额外资源需求（总运输机 ≥ 9 > 8），缺口天然 ≥ 1。
3. **缺口最小化 → 让被孤立的那一组尽可能小**：额外资源 = 被孤立组所需的资源。要让缺口最小，就是把一个「需求最小的单服务区」独立成组。
4. **S011（块 B08）是需求最小的可孤立单元**：它只由一个 B 型架次 T013 服务（3 箱、直连 G01、无需中继）。孤立 S011 只需 +1 B 运输机 +1 B 电池，缺口 = 2，是 2 组下能达到的**最小缺口**。
5. **3 组同理**：在 14+1 基础上再孤立 S009（B07，1 个 B 架次 T014 + R01 中继保障），缺口增到 4。
6. 于是「缺口最小」这个第一目标，**把方案推向了「一个超级大组 + 若干孤立单点」的极端不均衡结构**。

一句话：**不均衡不是 bug，是「缺口优先、均衡最后」的目标顺序导致的必然结构。**

## 7. 是否数学正确

**是。**
- 枚举是穷举的（2 组 2047、3 组 86526 个分区），无遗漏；
- 字典序排序正确；
- 缺口 = max(0, 分区总配置 − 现有库存)，逐类计算正确；
- `top3` 候选显示：2 组第 2、3 名缺口为 3（孤立 B12/S015），3 组第 2、3 名缺口为 5，均大于最优，说明「14+1 / 13+1+1 确实是最小缺口」。
- 工作量不均衡值 = 1.0（归一化 W 的 max−min），已达理论上界，正是「大组 + 单点」结构的必然体现。

## 8. 是否符合原题

**部分符合。** 题目要求「给出方案并**比较**多维度（含均衡）」，未要求「最小化均衡」。当前把「缺口最小」放第一位，是**本队的目标优先级选择**，合理但不唯一。若直接宣称「这是 Q4 的最优分区」，而完全不谈均衡，会让评审误解。

## 9. 是否需要修改

- **模型/结果：无需修改**（结果正确）。
- **论文表述：需要补充**——（a）明示「缺口最小」是优先级选择；（b）诚实说明该选择导致的不均衡；（c）按题目要求，补一段「均衡性」的比较/讨论（已有 `q4_workload_comparison.csv` 数据支撑，无需重算）。

## 10. 论文应如何解释（可直接改写使用）

> 在「各组独立执行、资源不复用」的前提下，由于集中式方案恰好用满现有库存（缺口为 0），任何分区都必然引入额外资源。以「库存缺口最小」为第一目标时，最优策略是把一个资源需求最小的服务区独立成组，以最小化额外配置：2 组下孤立 S011（仅需 +1 架 B 型无人机与 +1 组 B 型电池，缺口 2），3 组下再孤立 S009（缺口增至 4）。因此本文给出的缺口最小方案呈现出「14+1」「13+1+1」的显著不均衡。需要强调，这一不均衡是**缺口最小化目标的直接结果，而非均衡优化失败**；题目要求的是从缺口、冗余、均衡等多维度比较分区方式，若以组间工作量均衡为首要目标，则应在目标函数中提高均衡权重另行求解。本文侧重回答「现有库存下分区执行会缺多少资源」这一核心问题，故采用缺口优先口径。

## 11. q4_workload_balance.png 是否适合正文

- 该图如实地反映了缺口最小方案的**极端不均衡**（组间架次 25 vs 1、货箱 77 vs 3），这正是题目要求「比较组间工作量均衡」的内容，**有存在价值**。
- 但**不建议作为「方案亮点」放正文**，否则评审会误以为作者在自曝「方案不均衡」而不加解释。
- 建议：放在「分区方式的特点分析/均衡性讨论」小节，配上面第 10 段的解释，明确它是「缺口优先口径下的均衡特征」，而非「已均衡优化」。
- 若正文篇幅紧张，可将其连同 `q4_workload_comparison.csv` 移入附录，正文用三线表给出 25 vs 1 等关键数字即可。
